<Page x:Class="Wpf08_GestorObjetosPerdidos.MVVM.View.PaginaEditarObjeto"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:Wpf08_GestorObjetosPerdidos.MVVM.ViewModel"
      Title="PaginaEditarObjeto">
    
    <Page.DataContext>
        <vm:EditarObjetoViewModel/>
    </Page.DataContext>

    <Grid 
        Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock 
            Grid.Row="0"
            Text="Editar / Agregar Objeto"
            FontSize="20"
            FontWeight="Bold"
            Margin="0,0,0,20"/>

        <Grid 
            Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock 
                Grid.Row="0" 
                Grid.Column="0"
                Text="Nombre:"
                VerticalAlignment="Center"
                Margin="0,0,10,8"/>
            <TextBox 
                Grid.Row="0" 
                Grid.Column="1"
                Text="{Binding Objeto.Nombre, UpdateSourceTrigger=PropertyChanged}"
                Margin="0,0,0,8"/>

            <TextBlock 
                Grid.Row="1" 
                Grid.Column="0"
                Text="Categoría:"
                VerticalAlignment="Center"
                Margin="0,0,10,8"/>
            <ComboBox 
                Grid.Row="1" 
                Grid.Column="1"
                ItemsSource="{Binding Categorias}"
                SelectedItem="{Binding Objeto.Categoria}"
                Margin="0,0,0,8"/>

            <TextBlock 
                Grid.Row="2" 
                Grid.Column="0"
                Text="Fecha:"
                VerticalAlignment="Center"
                Margin="0,0,10,8"/>
            <DatePicker 
                Grid.Row="2" 
                Grid.Column="1"
                SelectedDate="{Binding Objeto.FechaHallazgo}"
                Margin="0,0,0,8"/>

            <TextBlock 
                Grid.Row="3" 
                Grid.Column="0"
                Text="Ubicación:"
                VerticalAlignment="Center"
                Margin="0,0,10,8"/>
            <TextBox 
                Grid.Row="3" 
                Grid.Column="1"
                Text="{Binding Objeto.UbicacionHallazgo}"
                Margin="0,0,0,8"/>

            <TextBlock 
                Grid.Row="4" 
                Grid.Column="0"
                Text="Descripción:"
                VerticalAlignment="Top"
                Margin="0,0,10,0"/>
            <TextBox 
                Grid.Row="4" 
                Grid.Column="1"
                Text="{Binding Objeto.Descripcion}"
                TextWrapping="Wrap"
                AcceptsReturn="True"
                VerticalScrollBarVisibility="Auto"/>
        </Grid>

        <StackPanel 
            Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,20,0,0">
            <Button 
                Content="Volver"
                Command="{Binding VolverCommand}"
                Margin="0,0,10,0"
                Padding="15,6"/>
            <Button 
                Content="Guardar"
                Command="{Binding GuardarCommand}"
                Padding="15,6"
                Background="#4CAF50"
                Foreground="White"/>
        </StackPanel>
    </Grid>
</Page>
