<Window x:Class="TaskManagerDAM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManagerDAM"
        xmlns:vm="clr-namespace:TaskManagerDAM.MVVM.ViewModel" d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid 
        Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel 
            Grid.Row="0" 
            Orientation="Horizontal"
            
            Background="LightPink"
            HorizontalAlignment="Center">
            
            <TextBlock 
                Text="Nueva Tarea:" 
                VerticalAlignment="Center"/>
            <TextBox 
                Text="{Binding NewTaskTitle, UpdateSourceTrigger=PropertyChanged}" 
                Width="100" Margin="5"/>
            <Button 
                Style="{StaticResource AgregarStyle}"
                Content="Agregar" 
                Foreground="White"
                Command="{Binding AddTaskCommand}" 
                Width="60"
                Height="20"/>
        </StackPanel>

        <ListBox 
            Grid.Row="1" 
            ItemsSource="{Binding Tasks}" 
            Margin="0,10,0,0"
            Background="WhiteSmoke">

        </ListBox>
    </Grid>
</Window>
