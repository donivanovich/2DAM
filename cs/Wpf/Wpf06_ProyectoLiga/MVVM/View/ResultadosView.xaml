<Page x:Class="Wpf06_ProyectoLiga.MVVM.View.ResultadosView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Wpf06_ProyectoLiga"
      xmlns:VM="clr-namespace:Wpf06_ProyectoLiga.MVVM.Model"
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ResultadosView">

    <Page.DataContext>
        <VM:ResultadoModel/>
    </Page.DataContext>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding CargarTablaCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid 
        Background="#192234">
        <ScrollViewer 
            VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ListaResultados}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid 
                            Columns="2" 
                            Margin="30"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border 
                            Background="#232C41" 
                            CornerRadius="24" 
                            Margin="16">
                            <StackPanel 
                                VerticalAlignment="Center" 
                                HorizontalAlignment="Center" 
                                Margin="18">
                                <TextBlock Text="{Binding FechaHora, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" Foreground="White" FontSize="24" TextAlignment="Center" Margin="0,0,0,8"/>
                                <Image Source="{Binding EscudoLocal}" Height="60" Margin="0,0,0,6" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding NombreLocal}" Foreground="White" FontWeight="SemiBold" FontSize="20" HorizontalAlignment="Center"/>
                                <Border Background="#394055" CornerRadius="14" Margin="0,16" Padding="18,4">
                                    <StackPanel 
                                        Orientation="Horizontal" 
                                        HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding GolesLocal}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                                        <TextBlock Text=" - " Foreground="White" FontSize="28" Margin="8,0"/>
                                        <TextBlock Text="{Binding GolesVisitante}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                                <Image Source="{Binding EscudoVisitante}" Height="60" Margin="0,0,0,6" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding NombreVisitante}" Foreground="White" FontWeight="SemiBold" FontSize="20" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
